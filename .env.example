# Redis connection string. Special characters in password ($, ;, |, %, etc.) must be URL-encoded (e.g., $ -> %24, ; -> %3B, | -> %7C, % -> %25)
REDIS_URL=redis://default:password@host:port
JWT_SECRET=your_jwt_secret_key_here
